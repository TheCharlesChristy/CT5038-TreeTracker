{
  "name": "tree-tracker-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "bootstrap.js",
  "engines": {
    "node": "17.9.1"
  },
  "scripts": {
    "dev": "node bootstrap.js",
    "dev:watch": "nodemon bootstrap.js",
    "start": "node bootstrap.js",
    "lint": "npm run db-lock:check",
    "db-lock:check": "node scripts/enforce-db-lock.js",
    "test": "node --test",
    "test:integration": "RUN_DB_TESTS=true node --test test/integration.db.test.js",
    "check": "npm run lint && npm test"
  },
  "dependencies": {
    "mysql2": "^3.14.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}