{
  "name": "tree-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "install:all": "npm install --prefix server && npm install --prefix TreeGuardiansExpo",
    "build": "cd TreeGuardiansExpo && npx expo export --platform web"
  },
  "dependencies": {
    "expo": "^55.0.9"
  },
  "devDependencies": {
    "@types/react": "~19.2.10",
    "typescript": "~5.9.2"
  }
}
